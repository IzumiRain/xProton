#!/usr/bin/env python3
"""xProton — multi-location ProtonVPN on one server, SOCKS5 per location."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))

from xpl.cli import main

if __name__ == "__main__":
    sys.exit(main())
