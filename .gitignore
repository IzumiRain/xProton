# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# local runtime state (never commit)
/etc/xproton/
accounts.txt
manual-accounts.txt

# internal / agent / dev tooling (never commit) — generic patterns, no specifics
.freebuff/
*Research*.md
*.ps1
*-heavy.sh
dev-ssh*

# secrets / credentials (never commit)
*.pem
*.key
*id_rsa*
*.env

# test artifacts
*.conf.json